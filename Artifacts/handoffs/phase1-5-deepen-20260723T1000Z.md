# Handoff — Phases 1–5 deepen push — 2026-07-23T1000Z

## Outcome
INCOMPLETE overall (signing/notarize + manual VoiceOver still open)

## Landed this push
- Phase 2: embed `ChromeNativeHost` in app; extension selection/takeover-off-by-default/host health UX
- Phase 3: `YtdlpJSONProbe` + vendor fetch script (skips until URL+sha256 filled)
- Phase 4: `MetalinkParser` + torrent path/case-fold hardening (FR-P2P-007 / FR-MSRC)
- Phase 1: accessibility evidence sheet (manual VO still NOT RUN)
- Phase 5: `make release-sbom` local artifact

## Verified
- `make verify-fast` OK — **185** unit tests
- `make vendor-media-helpers` skips (pins without download URLs)
- ChromeNativeHost listed under app `Contents/MacOS/` after Debug build

## Still blocked / not done
- Developer ID codesign + notarize + Sparkle keys
- Interactive VoiceOver PASS evidence
- Bundled yt-dlp/ffmpeg binaries (need checksummed URLs)
- libtorrent / magnet transfer (ADR 0006)
