# Handoff — plans/001–013 implementation

**COMPLETE**

All fourteen plan files under `plans/` (README + 001–013) are implemented.
`plans/README.md` marks every row **DONE**.

## What landed

| Plan | Outcome |
|---|---|
| 001 | `native-dependencies: vendor-libcurl` on DownloadKit-linked Makefile targets; CI full gate → `make verify` |
| 002 | `RangeProbeOutcome` / `observeRangeProbe`; probe `bodyByteLimit: 1`; structural `bytesWritten == 1` assertion |
| 003 | `RangeProbePolicy` gates Cookie / Authorization / userpwd / cookie jar + fragile signed query (cloud exemptions kept); orchestrator + SegmentedTransfer |
| 004 | Pre-`pwrite` Content-Range validation in C; ranged HTTP success = 206 only; `invalidRangeResponse` |
| 005 | `SegmentLedger.load` rejects structurally invalid maps (gaps/overlaps/unsorted/coverage) without rewriting sidecar |
| 006 | Header metadata reset at HTTP response boundaries; terminal redirect identity only |
| 007 | Origin-scoped redirect headers; drop Cookie/Authorization/Referer cross-origin; reject HTTPS→HTTP |
| 008 | Opaque C11 atomic `DMCurlAbortFlag` end-to-end (no volatile Int32 / `@unchecked Sendable` abort token) |
| 009 | Typed `JobState` transitions in `JobRepository`; named `requeueInterruptedTransfers` / `requeueActiveTransferJob` |
| 010 | Migration `v6-finalization-intents`; promote intent before rename; crash reconcile without blind requeue / file wipe |
| 011 | `PartialRestartPolicy` + sibling replacement restart for no-range partials; preallocated shells fail closed |
| 012 | `incomplete-work-scan` root includes `BrowserExtension` |
| 013 | Deterministic `dropFirst=N` loss schedule + typed counters; correctness asserts drops |

## Evidence (this session)

```
make format-check                 → 0/223 files require formatting
make incomplete-work-scan         → clean
make test-unit                    → 432 passed
make verify-fast                  → OK
make test-integration             → 59 passed
make test-recovery                → 9 passed
make test-asan                    → TEST SUCCEEDED (unit+integration+recovery; no AddressSanitizer findings in tail)
make test-tsan                    → TEST SUCCEEDED (exit 0; no ThreadSanitizer data-race lines in tail)
make db-migration-test            → 9 passed (incl. v6 finalization intents)
```

## Not done / caveats

- **No commit** — tree stays dirty; staged Artifacts/release zips from prior work are unrelated and should not ship with this slice.
- Full **`make verify`** (analyze + fuzz + audit + evidence bundle) was not run end-to-end in this pass; the transfer-critical lanes above are green.
- Local SwiftLint may be absent → `make lint` can pass via grep backstop.

## Operator next steps

1. Review diff (exclude release Artifacts noise).
2. Optionally `make test-tsan` / `make verify` before merge.
3. Explicit authorization required before commit/push.
