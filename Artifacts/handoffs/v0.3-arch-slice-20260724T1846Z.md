# Handoff — v0.3 / architecture slice — 2026-07-24T1846Z

## Outcome
**INCOMPLETE** — High-ROI slice of v0.3 + architecture P0–P2 landed and
`make verify-fast` is green. Remaining v0.3 Phase 1 items (S6 TLS share, S8
progress coalesce, cancel-and-split live steal, UX U4–U11, deletes D2+) and
architecture P3+ are not done. No commit requested for this slice.

## Baseline
- Plan baseline: `ccdacdc` (`main`)
- Working tree: uncommitted (see `git status`)

## Verified
- `make verify-fast`: **OK** — 226 unit tests
  Artifact: `Artifacts/validation/latest/unit-tests.xcresult`
- `make incomplete-work-scan`: clean (via verify-fast)
- Comparator self-test: `Scripts/performance-compare.sh` fails closed on >10%
  regression

## Delivered this slice

### Architecture / gates
- **P0 (partial):** `Scripts/performance-compare.sh` + `make performance-compare`;
  `performance-baseline.sh` writes immutable `candidate-<stamp>.json` (never
  overwrites approved baselines). Numeric XCTest extraction still open.
- **P1:** Replaced 24 unbounded per-RPC caches with one
  `RequestReplayStore` (count 256 / age 15m / bytes 32MiB + mutation receipt
  ring 4096). Reads recompute on miss; mutations fail closed after receipt
  eviction (`duplicateRequestID` / `"idempotency receipt expired"`).
- **P2 / S5:** `JobRepository.fetchJobRows` constant-statement plan (jobs +
  batched resources/categories/projects + one tag join). Statement-count unit
  test asserts ≤10 statements for 20 tagged jobs (measured 7).

### Transfer speed (v0.3)
- **S1 (queue refill):** `CurlMultiLoop` drains `CURLMSG_DONE` mid-loop and
  refills from a pending queue under `maxConcurrent`. Large files tile into
  ~4 MiB chunks (≤128) while live connections stay at `preferredSegmentCount`.
  Not full cancel-and-split of a live slow range (plan’s stricter steal).
- **S2:** Socket grant before `connecting`/`downloading` (stay queued on miss).
- **S3:** Stop poisoning host `maxSegments` from small downloads.
- **S4:** Drop per-easy `fsync` in C; one Swift `synchronizeFile` after map/single.
- **S7 partial:** `resplit` path keeps host segment clamp via `connectionLimit`.

### UX (partial)
- Adaptive Library poll (500 ms live / 5 s idle).
- Error banner + engine-unavailable empty state with Start Engine.

### Misc
- User-Agent → `Flow/0.2.0`
- Makefile: `performance-compare`; removed duplicate `performance-baseline`
- FIRST_PARTY scan path already points at `BrowserExtension` (prior)

## Explicitly not done
- True IDM-style cancel/split steal + capped-tail acceptance benchmark
- S6 `CURLSH` DNS/SSL share; S8 progress Task coalesce
- Architecture P3 change-aware listJobs; P4 progress module; P5 scheduler
- UX U4–U11, deletion D2+ (dead LaunchAgent paths, copy pass, a11y)
- Commit / release / notarization

## Next recommended grab
1. S6 shared curl session + single probe (time-to-first-byte).
2. S8 progress coalesce (actor hop bound).
3. Or UX error/empty polish U4–U6 if shipping feel matters more than throughput.
4. Commit this working tree when maintainer authorizes.
